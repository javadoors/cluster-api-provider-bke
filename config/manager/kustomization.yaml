resources:
- manager.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: ologger-config
  files:
  - ../ologger/ologger.yaml

#configMapGenerator:
#- name: manager-config
#  files:
#  - controller_manager_config.yaml
